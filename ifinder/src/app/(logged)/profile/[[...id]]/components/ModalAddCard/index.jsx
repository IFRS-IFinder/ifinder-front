import { useAddCard } from "./useAddCard.hook"

export function ModalAddCard(){
    const {errors, handleSubmit, onSubmit, register, isLoading, errorAddCard} = useAddCard();

    return(
        <div>
            
        </div>
    )
}